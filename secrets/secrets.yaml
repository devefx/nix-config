# Encrypted secrets file (managed by sops)
# To edit: sops secrets/secrets.yaml
# To create: sops --encrypt --in-place secrets/secrets.yaml
#
# Example plaintext (before encryption):
# example-secret: my-secret-value
# db:
#     password: super-secret
